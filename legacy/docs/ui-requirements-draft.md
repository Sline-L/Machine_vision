# 早期 UI 需求草稿

> 状态：历史资料。当前实现和使用方式以项目根目录 `README.md` 为准。

弄清楚 `new1.py` 的逻辑，可以开始封装文件。

统一工程命名规则：`gp` Python 包，根入口为 `gp_main.py`。

对于UI界面的设计
初始第一个界面

核心界面：
- 开始运行按钮
- 当前已经检测零件数，其中合格数、不合格数
- 右上角设置
- 右侧显示栏，上方显示当前设置，下方显示检测结果和图表

设置界面。
